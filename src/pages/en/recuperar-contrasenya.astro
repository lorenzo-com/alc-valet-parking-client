---
import Layout from "@/layouts/Layout.astro";
import RecoveryPassword from "@/components/auth/RecoveryPassword.svelte";
import { getLangFromUrl } from "@/i18n/utils";

const lang = getLangFromUrl(Astro.url);
---

<Layout title="Recuperar Contraseña | Tu Empresa">
  <RecoveryPassword client:load lang={lang} />
</Layout>
